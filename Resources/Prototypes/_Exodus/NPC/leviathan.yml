# Ctrl C + Ctrl V from MeleeCombatCompound
# modifier for leviathan

- type: htnCompound
  id: LeviathanCompound
  branches:
  - tasks:
      - !type:HTNCompoundTask
        task: LeviathanMeleeCombatCompound
  - tasks:
      - !type:HTNCompoundTask
        task: FollowCompound
  - tasks:
      - !type:HTNCompoundTask
        task: IdleCompound

- type: htnCompound
  id: LeviathanMeleeCombatCompound
  branches:
    - tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UtilityOperator
            proto: NearbyLeviathanMeleeTargets
        - !type:HTNCompoundTask
          task: LeviathanBeforeMeleeAttackTargetCompound

- type: htnCompound
  id: LeviathanBeforeMeleeAttackTargetCompound
  branches:
    - preconditions:
        - !type:BuckledPrecondition
          isBuckled: true
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UnbuckleOperator
            shutdownState: TaskFinished

    - preconditions:
        - !type:PulledPrecondition
          isPulled: true
      tasks:
        - !type:HTNPrimitiveTask
          operator: !type:UnPullOperator
            shutdownState: TaskFinished

    - preconditions:
        - !type:InContainerPrecondition
          isInContainer: true
      tasks:
        - !type:HTNCompoundTask
          task: EscapeCompound

    # Pickup weapon if we don't have one.
    - preconditions:
        - !type:ActiveHandComponentPrecondition
            components:
              # Just serializer things
              - type: MeleeWeapon
                damage:
                  types:
                    Blunt: 0
            invert: true
      tasks:
        - !type:HTNCompoundTask
          task: PickupMeleeCompound

    # Melee combat (unarmed or otherwise)
    - tasks:
        - !type:HTNCompoundTask
          task: MeleeAttackTargetCompound

- type: utilityQuery
  id: NearbyLeviathanMeleeTargets
  query:
    - !type:NearbyHostilesQuery
  considerations:
    - !type:TargetIsAliveCon
      curve: !type:BoolCurve
    - !type:TargetDistanceCon
      curve: !type:PresetCurve
        preset: TargetDistance
    - !type:TargetHealthCon
      curve: !type:PresetCurve
        preset: TargetHealth
    # - !type:TargetAccessibleCon
    #   curve: !type:BoolCurve
    # - !type:TargetInLOSOrCurrentCon
    #   curve: !type:BoolCurve
